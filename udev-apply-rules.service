[Unit]
Description=Apply udev rules for /dev/ttyTHS1
After=dev-ttyTHS1.device
Requires=dev-ttyTHS1.device

[Service]
Type=oneshot
ExecStart=/bin/bash -c "udevadm control --reload-rules && udevadm trigger /dev/ttyTHS1"

[Install]
WantedBy=multi-user.target